var configOptions = {
    tenant: '', // Tenant id (GUID or domain) or "common"
    clientId: '', // Application ID, from https://apps.dev.microsoft.com
    postLoginRedirectUri: 'http://localhost', // URI to redirect after authentication
}
